apiVersion: v1
kind: Service
metadata:
  name: {{ include "dsipsmule-voter.fullname" . }}
  labels:
    {{- include "dsipsmule-voter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dsipsmule-voter.selectorLabels" . | nindent 4 }}
